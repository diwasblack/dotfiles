#!/bin/bash

# Script to hold the global variables

# Detect and store package manager info
# For now use apt
export pkg_mgr_install="sudo apt install -y"
export pkg_mgr_repo_update="sudo apt update"
